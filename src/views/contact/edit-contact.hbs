<div class="col-md-4 mx-auto">
    <div class="card">
        <div class="card-header">
            <h3>Editar Contacto</h3>
        </div>
        <div class="card-body">
            <form action="/contact/edit-contact/{{contact._id}}?_method=PUT" method="POST">
                {{!-- Utilizamos un input oculto para que el formulario envie una peticion PUT al servidor, asi aunque veamos que el metodo es POST lo envia como un metodo PUT gracias a "method override" --}}
                <input type="hidden" name="_method" value="PUT" />
                <div class="form-group">
                    <input type="text" class="form-control borde" name="nombre" placeholder="Nombre"
                        value="{{contact.nombre}}" />
                </div>
                <div class="form-group">
                    <input type="text" class="form-control borde" name="numero" placeholder="Numero"
                        value="{{contact.numero}}" />
                </div>
                <div class="form-group">
                    <button class="btn btn-primary btn-block" type="submit">Guardar</button>
                </div>
            </form>
        </div>
    </div>
</div>